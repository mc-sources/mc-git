/**
 * Technical configuration for the mcgit application.
 * These values are set at build time and are not user-editable.
 */

/** Email address used as the recipient for user feedback. */
export const FEEDBACK_EMAIL = "mcgit-feedback@martingatignol.fr";
